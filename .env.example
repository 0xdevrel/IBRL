# Server-side (agent + API routes)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# Optional failover list (comma-separated). Kept server-side and never shipped to the browser.
# SOLANA_RPC_URLS=https://mainnet.helius-rpc.com/?api-key=...,https://solana-mainnet.g.alchemy.com/v2/...
HACKATHON_API_KEY=your_hackathon_api_key
GEMINI_API_KEY=
# Gemini model override (optional)
# GEMINI_MODEL=gemini-3-flash-preview

# Local SQLite database (server-side only). Do not commit this file to git.
SQLITE_PATH=.local/ibrl.sqlite

# Agent signer (server-side only)
# JSON array format: [1,2,3,...]
AGENT_PRIVATE_KEY=[]

# Client-side RPC (used by wallet adapter in the browser)
# Recommended: keep vendor keys server-side by using the built-in proxy:
NEXT_PUBLIC_SOLANA_RPC_URL=/api/rpc
